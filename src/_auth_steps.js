/**
 * 1. create project in console.firebase.com and register
 * 2.install firebase in project
 * 3. create firebase.init.js and import auth(app) export auth
 * 4. auth-enable emailPass/ google
 */
/**
 * hosting steps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
 */
